{
  "buildCommand": "cd $VERCEL_PROJECT_ROOT && npx expo export --platform web --output-dir dist/web",
  "outputDirectory": "dist/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
